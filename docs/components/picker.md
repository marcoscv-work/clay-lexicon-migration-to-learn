---
title: Picker
description: A picker looks like a select but opens a fully styled, accessible option panel.
sources:
  - https://www.clayui.com/docs/components/picker
storybook: https://storybook.clayui.com/?path=/story/design-system-components-picker--default
status: poc-draft
api_version: 3.165.0
---

# Picker

Use the picker when you need the behavior of a [select](/components/select)
with a fully styled panel: the user opens it, chooses an option, and the button
shows the selection. Unlike a native select, the picker's options can be styled
and composed; unlike a [drop down](/components/drop-down), it holds a value
rather than actions.

<!-- REVIEW: the Picker ships in @clayui/core as Beta and has no standalone Lexicon spec; the selector design rules apply. Confirm status with the Clay team. -->

## Variants and anatomy

The picker pairs a trigger button with an options panel built from `Option`
items. It supports a search field inside the panel for long lists, custom
triggers, grouped options, and a shrink variant that sizes the trigger to its
content.

Try the [Picker story](https://storybook.clayui.com/?path=/story/design-system-components-picker--default),
the [search story](https://storybook.clayui.com/?path=/story/design-system-components-picker--search),
and the [custom options story](https://storybook.clayui.com/?path=/story/design-system-components-picker--custom-options).

## Usage guidelines

- Prefer the native [select](/components/select) when its styling is enough;
  reach for the picker when options need rich content or the panel needs
  search.
- Label the picker like any form control.
- Keep options as values, not actions; actions belong in a drop down.

## Implementation

The component ships in `@clayui/core`:

```shell
npm install @clayui/css @clayui/core
```

```jsx
import {Option, Picker} from '@clayui/core';
import ClayForm from '@clayui/form';

export default function Example() {
	return (
		<ClayForm.Group>
			<label htmlFor="year" id="yearLabel">
				Year
			</label>
			<Picker
				aria-labelledby="yearLabel"
				id="year"
				items={['2024', '2025', '2026']}
			>
				{(item) => <Option key={item}>{item}</Option>}
			</Picker>
		</ClayForm.Group>
	);
}
```

## API reference

<!-- API:START package=@clayui/core version=3.165.0 -->
<!-- Generated by scripts/generate-api.mjs. Do not edit by hand; run `npm run api:generate`. -->

### Picker

| Prop | Type | Default | Required | Description |
| ---- | ---- | ------- | -------- | ----------- |
| `UNSAFE_behavior` | `'secondary'` |  | No |  |
| `UNSAFE_menuClassName` | `string` |  | No | Sets the className for the React.Portal Menu element. |
| `active` | `boolean` |  | No | Flag to indicate if the DropDown menu is active or not (controlled). |
| `aria-describedby` | `string` |  | No | The global `aria-describedby` attribute identifies the element that describes the component. |
| `aria-label` | `string` |  | No | The `aria-label` attribute defines a string value that labels an interactive element. |
| `aria-labelledby` | `string` |  | No | The `aria-labelledby` attribute identifies the element (or elements) that labels the element it is applied to. |
| `as` | `'button' \| React.ForwardRefExoticComponent<any> \| ((props: React.HTMLAttributes<HTMLElement>) => JSX.Element)` |  | No | Custom trigger component. |
| `className` | `string` |  | No | Sets the CSS className for the component. |
| `defaultActive` | `boolean` | `false` | No | Property to set the default value of `active` (uncontrolled). |
| `defaultSelectedKey` | `React.Key` |  | No | The initial selected key (uncontrolled). |
| `direction` | `'bottom' \| 'top'` | `'bottom'` | No | Direction the menu will render relative to the Picker. |
| `disabled` | `boolean` |  | No | Flag to indicate that the component is disabled. |
| `displayKeyboardArrowsIndicator` | `boolean` | `false` | No | Flag to render the `KeyboardArrowsIndicator` alongside the Picker trigger, hinting that up and down arrow keys can be used to navigate the option list. The indicator floats to the right of the trigger and flips to the left when it would overflow the viewport. It is only rendered while the menu is open. |
| `filterKey` | `string` |  | No | Defines the name of the property key that is used in the items filter test. |
| `id` | `string` |  | No | The id of the component. |
| `messages` | `Partial<PickerMessages>` |  | No | Messages for the Picker. |
| `native` | `boolean` | `false` | No | Flag to make the component hybrid, when identified it is on a mobile device it will use the native selector. |
| `onActiveChange` | `InternalDispatch<boolean>` |  | No | Callback for when the active state changes (controlled). |
| `onSelectionChange` | `InternalDispatch<React.Key>` |  | No | Callback calling when an option is selected. |
| `placeholder` | `string` | `'Select an option'` | No | Text that appears when you don't have an item selected. |
| `searchable` | `boolean` |  | No | Flag to indicate if the component should be searchable. |
| `searchableThreshold` | `number` |  | No | The threshold of items to show the search input. |
| `selectedKey` | `React.Key` |  | No | The currently selected key (controlled). |
| `shrink` | `boolean` |  | No | Flag to make the picker only as wide as its contents. |
| `width` | `number` |  | No | Sets the width of the panel. |

<!-- API:END -->

## Accessibility

- The picker implements the ARIA listbox pattern over a combobox-style trigger:
  arrow keys move through options, typing jumps to matches, and `Enter`
  selects.
- Associate the label through `aria-labelledby`, as with a native select.
- The selected option is exposed on the trigger, so the current value is always
  announced.

## Live examples

See the [Picker stories on Storybook](https://storybook.clayui.com/?path=/story/design-system-components-picker--default).

## Related components

- [Select](/components/select)
- [Drop Down](/components/drop-down)
- [Forms](/components/forms)
