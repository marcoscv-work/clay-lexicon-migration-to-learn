---
title: Select
description: A select lets the user pick one option from a native list of provided options.
sources:
  - https://design.liferay.com/lexicon/core-components/forms/selector/
  - https://www.clayui.com/docs/components/select
storybook: https://storybook.clayui.com/?path=/story/design-system-components-select--high-level
status: poc-draft
api_version: 3.165.0
---

# Select

Use a select to pick one option from a fixed list provided by the system. A
select is a native form control with a data provider; it is not a
[drop down](/components/drop-down), which opens a panel that can contain
arbitrary content and actions.

## Variants and anatomy

A select shares its states with the [text input](/components/input): default,
focused, disabled, read only, and the success, warning, and error validation
states.

For moving many options between two lists, use the
[Dual Listbox](/components/dual-listbox); for tag style multiple selection with
autocomplete, use [Multi Select](/components/multi-select).

Try the [Select story](https://storybook.clayui.com/?path=/story/design-system-components-select--high-level)
and the [select box story](https://storybook.clayui.com/?path=/story/design-system-components-selectbox--default),
a listbox variant that shows several options at once.

## Usage guidelines

- Use a select when the user must choose one of a known, reasonably short list
  of options. For very long lists, prefer a searchable control such as an
  autocomplete.
- Label every select, and give options short, distinct names.
- Do not use a select where a drop down of actions belongs: selects hold data
  values, not commands.

## Implementation

The select ships in `@clayui/form`:

```shell
npm install @clayui/css @clayui/form
```

Use the high level component with an `options` array, inside a form group:

```jsx
import ClayForm, {ClaySelectWithOption} from '@clayui/form';

export default function Example() {
	return (
		<ClayForm.Group>
			<label htmlFor="region">Region</label>
			<ClaySelectWithOption
				id="region"
				options={[
					{label: 'Europe', value: 'eu'},
					{label: 'Americas', value: 'am'},
					{label: 'Asia Pacific', value: 'ap'},
				]}
			/>
		</ClayForm.Group>
	);
}
```

For full control, compose `ClaySelect` with `ClaySelect.Option` children.

## API reference

<!-- API:START package=@clayui/form version=3.165.0 -->
<!-- Generated by scripts/generate-api.mjs. Do not edit by hand; run `npm run api:generate`. -->

### ClaySelect

| Prop | Type | Default | Required | Description |
| ---- | ---- | ------- | -------- | ----------- |
| `shrink` | `boolean` |  | No | Flag to make the Select component only as wide as its contents. |
| `sizing` | `'lg' \| 'sm'` |  | No | Set the proportional size of the Select component. |

### ClaySelectBox

| Prop | Type | Default | Required | Description |
| ---- | ---- | ------- | -------- | ----------- |
| `ariaLabels` | `{ reorderDown: string; reorderUp: string; }` | `{ reorderDown: 'Reorder Down', reorderUp: 'Reorder Up', }` | No | Labels for aria attributes |
| `buttonAlignment` | `'center' \| 'end'` | `'end'` | No | Aligns the buttons used to reorder items relative to the Select Box. |
| `disabled` | `boolean` |  | No | Disables the component. |
| `items` | `Array<TItem>` |  | Yes | Items to be displayed in the Select Box. |
| `label` | `string` |  | No | Label to be displayed above the Select Box. |
| `multiple` | `boolean` |  | No | Defines whether the Select Box supports selection of multiple items. |
| `onItemsChange` | `(items: Array<TItem>) => void` |  | No | Handler that triggers when the content of the items prop are changed caused by reordering of items. |
| `onSelectChange` | `(value: Array<string>) => void` |  | Yes | Handler that triggers when a new item is selected. |
| `selectedIndexes` | `Array<number>` |  | No | Selected indexes, most commonly used in combination with the Dual Listbox component |
| `showArrows` | `boolean` |  | No | Defines whether the component should render buttons that allow reordering of items. |
| `size` | `number` |  | No | Amount of items that can fit inside the both Select Boxes before a scrollbar is introduced. |
| `spritemap` | `string` |  | No | Path to the spritemap that Icon should use when referencing symbols. |
| `value` | `string \| Array<string>` |  | Yes | Value of the component. |

<!-- API:END -->

## Accessibility

- The select renders the native element, so keyboard and screen reader behavior
  come from the browser; keep it that way unless you have a strong reason.
- Associate the label with the control, and keep the first option meaningful
  (or use an explicit placeholder option marked as disabled).

## Live examples

See the [Select stories on Storybook](https://storybook.clayui.com/?path=/story/design-system-components-select--high-level).

## Related components

- [Drop Down](/components/drop-down)
- [Multi Select](/components/multi-select)
- [Dual Listbox](/components/dual-listbox)
